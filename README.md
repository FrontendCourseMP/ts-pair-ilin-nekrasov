# Наша команда:
|Имя команды|
|-- |
|__cool boys__ (ts-pair-ilin-nekrasov)|

| Участники команды | Ник |
|-- | -- |
| Артём Ильин | nmktthh |
| Некрасов Алексей | nelexey |

# Критерии:

Критерии выбора библиотек для валидации форм на __TypeScript__ включали:
- поддержка строгой типизации __TypeScript__ и возможность автоматического вывода типов, что важно для устойчивости и удобства разработки на TS;
- __специализация на валидации форм__ или удобная интеграция с формами, чтобы обеспечить простое и гибкое управление валидацией именно пользовательских форм;
- __легковесность__ и __минимальное количество внешних зависимостей__ для оптимальной производительности и быстрого отклика интерфейса.

### Наш выбор:

Библы для валидации форм, наш топ:
| № | Библиотека | Причина добавления |
| -- | -- | -- |
| 1 | [__Zod__](https://github.com/colinhacks/zod) | популярна, лучшая интеграция с TypeScript, легкая |
| 2 | [__JustValidate__](https://github.com/horprogs/Just-validate) | 	простая валидация форм, удобство |
| 3 | [__Yup__](https://github.com/jquense/yup) | популярна, гибкая, хороша для React |
| 4 | [__FormValidation__](https://github.com/form-validation/) | легковесная, минимальные зависимости |
| 5 | [__SuperStruct__](https://github.com/ianstormtaylor/superstruct) | легкая, декларативная, для простых задач |

Мы выбираем JustValidate, она довольно простая и удобная, а также удовлетворяет все наши потребности 🐈‍⬛.

# Точка входа:
Для начала необходимо скачать библиотеку
- npm install just-validate --save

После импортируем:
- import JustValidate from 'just-validate';

либо через вставку в html: 
`<script src="https://unpkg.com/just-validate@latest/dist/just-validate.production.min.js"></script>`
с использованием через window.JustValidate

# Основные используемые классы и методы:
- __JustValidate__ - главный класс для создания валидатора формы
- метод конструктора:
new JustValidate(form, options) - инициализация валидатора с указанием формы (селектор или DOM-элемент) и опциональных настроек

### Минимальный набор необходимых методов для работы с формой:
- .addField(selector, rules) - добавляет поле для валидации с набором правил

- .onSuccess(callback) - задаёт функцию, вызываемую при успешной проверке формы

- .onFail(callback) - задаёт функцию, вызываемую при ошибках валидации

### Часто используемые правила валидации для полей:
- { rule: 'required' } - поле обязательно для заполнения

- { rule: 'email' } - проверка на корректность email

- { rule: 'minLength', value: число } - минимальная длина строки

- { rule: 'maxLength', value: число } - максимальная длина строки

- { rule: 'number' } - допустимы только цифры

- { rule: 'password' } - проверка надёжности пароля

### Дополнительные полезные методы:
- .revalidate() - повторный запуск проверки формы вручную

- .destroy() - удаление валидатора с формы

- .setCurrentLocale(locale) - установка языка для сообщений об ошибках (локализация)

### Типы данных
- form - строка-селектор элемента формы или сам DOM-элемент

- rules - массив объектов, каждый содержит правила для поля

- options - объект с настройками (например, подсказки, автофокус и др.)

#### В наших проектах, где мы делали валидацию ФИО и форму калькулятора с выражениями из цифр и операторов (+, *), пригодились бы такие методы JustValidate:
- addField(selector, rules) - для добавления полей с набором правил валидации

- onSuccess(callback) - для обработки успешной проверки формы

- onFail(callback) - для обработки ошибок при валидации

- revalidate() - для принудительной повторной проверки полей формы

- destroy() - для удаления валидатора с формы и очистки ресурсов

Хост - https://www.nmktth.ru/